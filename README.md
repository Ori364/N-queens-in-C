# N-queens-in-C
n queens solution in C (and priting the board), also n queens all possible solution + printing 
(the main difference is changing the recursive func returning value from a boolean to a void so it will keep looping through the collumns in each row until it fully looped and recursed the first row (0) )
